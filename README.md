# KNN CNN Demo Site

Static GitHub Pages version of the project demo.

Files:

- `index.html`
- `knn_cnn_demo_pool.js`
- `knn_cnn_outputs/demo_pool/...`

This site shows real validation images and the real cached predictions from the KNN on pretrained ResNet18 embeddings run.
